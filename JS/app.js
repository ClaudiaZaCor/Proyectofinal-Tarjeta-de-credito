//Pidiendo el numero de tarjeta de credito//
    var numberOfCreditCard = [prompt ("Inserte el numero de su tarjeta.")];
    numberOfCreditCard = [];

  //definiendo la longitud del array //
    if (numberOfCreditCard.length > 16) {
        numberOfCreditCard.length = 16
        };

    // aplicando reverse para elegir los numeros pares desde el final//
    var invertedNumber = numberOfCreditCard.reverse() ;
    var invertedNumber = i

    //Aplicando el algoritmo de Luhn//  
      var digit = parseInt(invertedNumber.charAt(i), 10);
        if (digit % 2 == 0 && digit <9 ) { //Definiendo la multiplicacion por 2 a los digitos pares menores que 9//
            (digit * 2) ;
            
      } else {
          (digit + (digit * 2)) // Ejecutando la suma de los digitos impares con el resultado anterior//
      
//Aplicando la funcion//
      var result = function isValidCard (digit, (digit * 2));
       return ((digit + (digit * 2)) % 10) == 0;
      }
  
  isValidCard (70)
